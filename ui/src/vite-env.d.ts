/// <reference types="vite/client" />

/**
 * Global constants injected at build time by Vite's `define` config.
 * @see vite.config.ts — define.__APP_VERSION__
 */
declare const __APP_VERSION__: string;
